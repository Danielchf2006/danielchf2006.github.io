# Resume

Drop the one-page PDF here, named exactly:

    Daniel-Cai-Resume.pdf

That filename is referenced by `src/data/site.ts` (`resumePath`) and linked
from the Experience page's "Download resume (PDF)" button. If you rename the
file, update `resumePath` to match.

Keep older versions out of `public/` (they'd be publicly downloadable) — archive
them somewhere else.
